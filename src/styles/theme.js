import { createTheme } from '@mui/material'

export const theme = createTheme({
  status: {
    danger: '#000000',
  },
  palette: {
    secondary: {
      main: "#f3ff6b", // Yellow
      light: "#ff0703",
      contrastText: "#ff0703",
    }
  }
});